<%- include("partials/header"); -%>

<h1>Home</h1>

<p> <%= homeLoremIpsum %> </p>

<!-- Display each composed entry, only display the first 100 chars -->
<% entries.forEach(function(entry) { %>
    <h1><%= entry.title %></h1>
    <p><%= entry.body.slice(0,99) + " ..." %>
    <a href="posts/:<%= entry.title %>">Read More</a>
    </p>
  <% }); %>

<%- include("partials/footer"); -%>